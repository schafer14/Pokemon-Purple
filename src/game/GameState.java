package game;

/**
 * A class that represents the state of a game (e.g., the positions of
 * each piece on a checkerboard together with an indication of whose move
 * it is.  Each game defines its own state, so this abstract class has
 * no interent state or methods.
 * 
 * @author Steven R. Vegdahl 
 * @version 2 July 2001 
 */
public abstract class GameState
{
	
}
